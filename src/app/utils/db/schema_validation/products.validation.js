export const DbValidator = async (data) => {
    let message = ""
    let is_error = false;
    let statusCode = 403;
    // for (let fields in data) {

    // }


    return {
        message,
        is_error,
        statusCode
    }

};